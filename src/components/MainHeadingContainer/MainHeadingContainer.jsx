import { MainHeadingContainerStyled } from './styled';

const MainHeadingContainer = ({ children }) => {
  return <MainHeadingContainerStyled>{children}</MainHeadingContainerStyled>;
};

export default MainHeadingContainer;
